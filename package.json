{
  "name": "graphql-apollo-server-github-scanner",
  "version": "1.0.0",
  "description": "A tool to scan GitHub repositories using GraphQL queries",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "codegen": "pnpm --stream -r run codegen",
    "start:build": "pnpm --stream -r start:build",
    "build:src": "pnpm --stream -r build:src",
    "start:dev": "pnpm --stream -r run start:dev",
    "clean": "rm -rf .parcel-cache && pnpm -r run clean"
  },
  "keywords": [
    "github",
    "graphql",
    "scanning"
  ],
  "author": "Dzianis Buyakou <dzianis.buyakou@gmail.com>",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
  }
}
